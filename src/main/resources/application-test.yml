spring:
  datasource:
    url: jdbc:h2:mem:retail-order-management-test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  security:
    jwt:
      secret: test-secret-key-for-jwt-phase-7a-123456
      issuer: retail-order-management-api-test
      expiration-minutes: 60
    bootstrap-user:
      username: admin
      password: admin123
      role: ADMIN
